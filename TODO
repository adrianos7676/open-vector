Add taskbar on Windows
Add a way to handle the keybind overlap check
Add settings saving
Add window focusing on linux (Wayland)
Add a struct for all input keys instead of 2 strings so refactor the InputKey struct
Add Keybind unbinding

Add basic tools
Tools:
-canvas drag tool (moving around with a mouse)

change all settings-related messages to use a single structed message